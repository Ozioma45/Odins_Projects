const reverseString = function(str) {
    mStr=str.split('').reverse().join('');//created a variable to hold the function and work on the string
    return mStr// here the string as be splited,reversed, and join to fit the tests
};

// Do not edit below this line
module.exports = reverseString;
